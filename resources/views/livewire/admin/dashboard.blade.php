<div><div class="metric-grid">@foreach($counts as $label=>$count)<article><small>{{ $label }}</small><strong>{{ $count }}</strong></article>@endforeach</div><section class="admin-panel mt-8"><div class="admin-panel-head"><h2>أحدث الطلبات</h2><a href="{{ route('admin.leads') }}">عرض الكل</a></div><div class="admin-table-wrap"><table><thead><tr><th>الاسم</th><th>الجوال</th><th>الخدمة</th><th>الحالة</th><th>التاريخ</th></tr></thead><tbody>@forelse($recentLeads as $lead)<tr><td>{{ $lead->name }}</td><td dir="ltr">{{ $lead->phone }}</td><td>{{ $lead->service?->name ?: 'غير محددة' }}</td><td><span class="status status-{{ $lead->status }}">{{ $lead->status }}</span></td><td>{{ $lead->created_at->diffForHumans() }}</td></tr>@empty<tr><td colspan="5">لا توجد طلبات بعد.</td></tr>@endforelse</tbody></table></div></section></div>
